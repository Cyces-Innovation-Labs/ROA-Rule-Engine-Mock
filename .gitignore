# Dependencies (local dev / server.js needs none; @netlify/blobs is
# deploy-only, see package.json + CLAUDE.md's "Deployment (Netlify)")
node_modules/

# Netlify CLI local state (site link, local Blobs cache, function bundles)
.netlify/

# Secrets / environment
.env
.env.local

# Logs
*.log
npm-debug.log*

# OS cruft
.DS_Store
Thumbs.db

# Editor — .vscode/launch.json currently hardcodes an absolute local path
# (d:\Cyces\...), so it's machine-specific rather than shared project config
.vscode/
.idea/
