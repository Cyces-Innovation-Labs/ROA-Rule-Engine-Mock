# Dependencies (project currently has none, but this is standard for a
# Node project and the server may grow real deps later)
node_modules/

# Secrets / environment
.env
.env.local

# Logs
*.log
npm-debug.log*

# OS cruft
.DS_Store
Thumbs.db

# Editor — .vscode/launch.json currently hardcodes an absolute local path
# (d:\Cyces\...), so it's machine-specific rather than shared project config
.vscode/
.idea/
